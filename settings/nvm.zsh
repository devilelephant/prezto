export NVM_DIR="$HOME/.nvm"
. "/usr/local/opt/nvm/nvm.sh"

#add current node to bin so MacVim can find it
#ln -fs `nvm which current` /Users/georgecoller/bin/node

#export NODE_PATH=dirname  `nvm which current`
#export $PATH=$PATH:$NODE_PATH
